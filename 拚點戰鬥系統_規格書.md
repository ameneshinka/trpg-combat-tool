# 拚點戰鬥系統 — 開發規格書

> 本文件是給 Claude Code 開發「KP 帶團用的戰鬥輔助網頁」的完整規格。
> 所有規則以本文件為準；若與原始規則書衝突，以本文件為準（原始規則書另有一份修訂清單待套用）。

---

## 0. 文件目的與使用方式

- 這是一個**狀態追蹤 + 數值結算引擎**：把一套自訂的、Limbus 風格的拚點戰鬥規則做成可在帶團時即時使用的網頁。
- 工具負責**確定性的運算**（最終威力、傷害、層數結算、混亂判定）。**不串接任何大語言模型**——所有計算都是確定性查表與算術。
- 核心原則：**規則寫在資料裡，引擎是通用的**。敵人、技能、被動、印記都是「填進引擎的資料」，新增內容不需改引擎程式碼。

---

## 1. 架構決策

| 項目 | 決策 |
|------|------|
| 前端託管 | GitHub Pages（靜態） |
| 狀態同步 | Firebase 即時資料庫（跨裝置） |
| 權威模型 | **KP 端權威**：玩家端只送「宣告／請求」，KP 裁定後才寫入正式狀態 |
| Firebase 安全規則 | 玩家只能寫自己的宣告欄位；正式戰鬥狀態只有 KP 能寫 |
| 開發分段 | **第一階段**先做單機版、把戰鬥計算做對；**第二階段**再接 Firebase 同步 |

> 安全提醒：Firebase 設定金鑰會公開在前端（正常），但**務必設安全規則**，否則任何開啟網頁者都能竄改戰鬥狀態。

---

## 2. 名詞定義（務必明確區分）

| 名詞 | 定義 |
|------|------|
| **戰鬥輪** | 整場戰鬥（從開打到結束）。專注力在「戰鬥輪開始」歸 0，跨回合保留 |
| **回合** | 戰鬥輪中的一個行動循環。層數在「回合結束」−1 |
| **層數** | 一個狀態「還能被觸發幾次」。初始 0；回合結束 −1；歸零時級數同步歸零 |
| **級數** | 一個狀態「被觸發時影響的程度」。不會自然增減，只由技能文本給予 |
| **硬幣類型** | 普通、紅、綠三種 |

> ⚠ 程式中 `戰鬥輪` 與 `回合` 必須是兩個不同的計數器，不可混用。

---

## 3. 資料模型

### 3.1 實體（PC / NPC）

```
Entity {
  id, name, isPC (bool), isControlledByKP (bool)
  hp, maxHp
  dex
  attributes { STR, INT, CON, SIZ, LUK }
  focus            // 專注力, 範圍 -40 ~ +40, 戰鬥輪開始為 0
  tempHp           // 臨時生命值, 回合結束歸零
  slots            // 本回合技能槽數
  skillLibrary[]   // 此角色擁有的所有技能（含「不可抽到、只能被喚出/解鎖」的技能）
  slotMap { A, B, C }   // 骰面群組 → 當前指向的技能 id（見 §4.3）
  slotMapRules[]   // 條件式改寫 slotMap 的規則（形態轉換／替換／解鎖）
  passives[]       // 被動清單（自由定義邏輯）
  states { }       // 每個狀態: { 層數, 級數 }
  confusionThresholds[]  // 混亂值位置（預設 maxHp*0.6, 0；震顫可上修）
  confusionLockTurns     // 混亂剩餘失去行動的回合數
  panicNextTurn (bool)   // 是否下回合進入恐慌
  panicRecovering (bool) // 恐慌剛結束，下回合開始要把 focus 歸 0
}
```

### 3.2 技能

```
Skill {
  id, name
  type             // "attack" | "defense"
  drawable         // bool: 預設 true。false = 不放進 slotMap 預設池，
                   //       只能被 slotMapRule 解鎖、或被 invokeSkill 效果喚出
  basePower        // 基礎威力
  coinPower        // 硬幣威力 (+N)
  coins[]          // 有順序的硬幣陣列，每枚: { type: 普通|紅|綠 }
  useEffects[]     // [使用時] 觸發的效果（含條件式）
  hitEffects[]     // [命中時] 觸發的效果
  conditions[]     // 條件式威力調整，如「若目標有印記X → basePower +Y」
}
```

- 硬幣的**排列順序很重要**：拚點失敗時依序從第一枚移除。
- 效果可引用印記 ID（見 §11）。
- **`invokeSkill` 是一種效果類型**（可放在 `useEffects` / `hitEffects` / 防禦技的「成功時」），用來**直接喚出**另一個技能打出去，繞過骰選。例：閃避「閃避成功時對攻擊者使用解兔」、被「預知」效果喚出技能三。

### 3.3 狀態

- 每個實體的 `states` 以「狀態名 → { 層數, 級數 }」儲存。
- 狀態分三種驅動方式（見 §10）：**層數驅動**、**級數驅動**、**純標籤（印記）**。

---

## 4. 技能槽規則

- **一個槽位** = 一次完整的「骰兩次 1d6、抽兩個技能、選一個」+ 一次行動。
- 一個實體可用多個槽位 → 參與多場拚點；**各槽位目標可分散**（可一槽打 A、一槽攔 B、一槽防守）。
- **攔截消耗一個槽位**（見 §9）。

### 4.1 PC 槽位（自動）

- 第一回合全體 = 1。
- 第二回合起，依「戰鬥輪開始前 KP 指定的順序」**輪流每人 +1**。
- 直到全隊槽位**總和 = 8**，封頂不再加。

範例（順序 A>B>C）：

| 回合 | A | B | C | 總和 |
|:--:|:--:|:--:|:--:|:--:|
| 1 | 1 | 1 | 1 | 3 |
| 2 | 2 | 1 | 1 | 4 |
| 3 | 2 | 2 | 1 | 5 |
| 4 | 2 | 2 | 2 | 6 |
| 5 | 3 | 2 | 2 | 7 |
| 6+ | 3 | 3 | 2 | 8（封頂） |

### 4.2 NPC 槽位（KP 手填）

- 所有 NPC（友方／敵方／boss）的槽位數，由 **KP 每回合手動填入數字**。
- 工具**不對 NPC 的數字做任何上限檢查**（8 的約束由 KP 自行掌握）。

### 4.3 動態技能對照表（slotMap）

> 這層讓「形態轉換／技能替換／解鎖／喚出」變得可表達，而**完全不改動 1d6 抽選核心**。

**核心拆解**：抽選不再直連技能，而是中間插一層對照表。1d6 永遠骰、永遠是 3:2:1，骰面只決定「命中哪個槽位」；「槽位裝著哪個技能」才是可變的。

```
1d6 → 骰面群組（永遠固定）：1/2/3 → 槽位A、4/5 → 槽位B、6 → 槽位C
                ↓ 每個槽位指向哪個技能 = 可被狀態/被動改寫
slotMap = { A: 技能id, B: 技能id, C: 技能id }
```

**三種操作全部統一成「改這張表」或「一個喚出效果」**：

| 操作 | 怎麼實現 |
|------|------|
| **替換** | `slotMapRule` 條件成立 → 把某槽位指向另一個技能（例：過熱時 A 從技能一改指技能四） |
| **解鎖** | `slotMapRule` 把原本空的／不可抽的技能放進某槽位（例：過熱時 C 改指技能七） |
| **喚出** | 不經 slotMap，由技能效果 `invokeSkill` 直接打出一個 `drawable=false` 的技能 |

**`slotMapRule` 資料結構**：

```
slotMapRule {
  condition        // 何時生效，例：自身帶有「預知眼過熱」、加速彈==0
  slot             // 要改哪個槽位：A / B / C
  setSkillId       // 改成指向哪個技能
  priority         // 多條同時成立時，高 priority 後蓋低 priority
}
```

**向下相容**：對 PC、木槿這種單純角色，`slotMap` 就固定是 `{A:技能一, B:技能二, C:技能三}`、沒有任何 `slotMapRule`，行為跟原本完全一樣。

**桌邊呈現**：對照表在「骰之前」就已套用完畢，玩家只看到**當下槽位指向的技能**（不會知道原本是哪個被替換掉）。

---

## 4A. 範例：瓦倫希娜的動態技能（驗證用）

說明這套模型如何表達「表面 3 種、實際 8 種」的角色：

- `skillLibrary` 放 8 個技能；技能三、四、七標 `drawable=false`（不進預設池）。
- 預設 `slotMap = {A:技能一, B:技能二, C:技能六}`。
- 「過熱回合結束 → 下回合 C 可換成技能七」= 一條 `slotMapRule`（condition: 帶過熱印記, slot: C, setSkillId: 技能七）。
- 「回合開始若無加速彈 → 技能五改用技能四」= 一條 `slotMapRule`。
- 技能三被防禦技「預知」的 `invokeSkill` 喚出、技能四被技能五的 `invokeSkill` 喚出 → 走喚出路徑，不進 slotMap。

---

## 5. 回合生命週期（六階段）

### 階段一：回合開始

0. **恐慌恢復**：對 `panicRecovering` 的實體，`focus = 0`（先於一切）。
1. **更新槽位**：PC 依 §4.1 自動；NPC 讀 KP 填入值。
2. **判定無法行動者**：`confusionLockTurns > 0` 或本回合恐慌者 → 跳過抽技能與宣告。
3. **結算技能對照表（slotMap）**：每個可行動實體——
   - 先把 `slotMap` 重置為角色預設；
   - 依序套用所有「condition 成立」的 `slotMapRule`（高 priority 後蓋低 priority）；
   - 得到本回合最終的 `{A, B, C}` 指向。
4. **抽技能**：每個可行動實體，依槽位數 N，重複 N 次：
   - 骰 1d6 兩次（**手動輸入**）。映射：1/2/3→槽位 A、4/5→槽位 B、6→槽位 C；再經 `slotMap` 換成當前指向的技能。
   - 菜單 = 兩次骰到的相異技能；**兩次同一個 → 只有一個選項（被迫單選，設計如此）**。
   - 從菜單選一個。
5. **回合開始被動結算**：如「隨機對無綁縛敵人施加綁縛」。隨機效果**工具自動骰、KP 可覆寫**。
6. **不動專注力**（只戰鬥輪開始才歸 0）。
7. **重算有效 DEX**（含迅捷／綁縛修正），供攔截判定。

### 階段二：宣告與配對

- 玩家／KP 宣告各槽位要「打誰」或「攔誰」。
- 工具判定攔截資格（見 §9），擋掉不合資格的攔截。
- 配對：每個攻擊技能綁定其最終拚點對象；無對象者標記為**單方面攻擊**。
- **KP 確認最終配對**後進入拚點。

### 階段三：拚點結算

見 §6（3a–3d）。

### 階段四：傷害結算

見 §7。

### 階段五：狀態觸發

見 §8（觸發時機對照表）。此階段非依序流程，而是各狀態在各自時機散觸發。

### 階段六：回合結束（依序）

1. **臨時生命值歸零**（先清，讓回合結束傷害打在真實血量）。
2. **回合結束觸發的被動與狀態結算**（延燒扣血、被動回專注、被動凝神判定…）。讀取的是**本回合完整、未扣層的狀態**。
3. **全體狀態層數 −1**。**例外：步驟 2 剛被加上的狀態豁免這一次**（留到下回合才開始扣）。
4. **更新持續時間與恢復**：
   - **混亂**：`confusionLockTurns -= 1`；歸 0 即恢復行動（血量不動，無額外結算）。
   - **恐慌進入判定**：若本回合 `focus` **跨越到 −40**（門鈴式，非停留），設 `panicNextTurn`。
   - **恐慌結束** → 設 `panicRecovering`（下回合開始歸 0 專注力）。

> 狀態生命週期的記憶法：**回合中（技能命中）貼上的會在回合結束褪掉（1 層當場消失）；回合結束（被動）才浮現的留到下回合。**

---

## 6. 拚點演算法（§3a–3d）

兩個攻擊型技能拚點。**整套拚點只決定「誰的技能通過」**，傷害在階段四算。

### 6a 單次交換

1. **雙方各自一次擲完當前所有硬幣**，數正面 → `最終威力 = 基礎威力 + (正面數 × 硬幣威力)`。
   - 每枚正面率 = `50% + 專注力%`（範圍 10%~90%）。**這類加權硬幣由工具自動擲**。
2. 最終威力**高者贏**這次交換。
3. **平手 → 重擲這次交換**（不損幣、不消耗硬幣），但**平手仍計入「拚點次數」**（影響專注力，見 6c）。
4. 輸方從**第一枚**開始損失一枚硬幣。
5. 重複 1–4，直到一方**可擲硬幣歸零** → 對方技能**通過**。
6. 通過方帶著**剩餘硬幣**進入階段四。

### 6b 勝負判定

- **判輸條件 = 可擲硬幣歸零（0 枚）**。
- 因此 1 枚硬幣的技能「輸不起任何一次交換」，但仍能拚（不是一進場就判輸）。

### 6c 專注力增減

- 變化量取決於**分出勝負 + 平手重擲**的交換總次數 k：`± (2k + 2)`（即 1 次 ±4、2 次 ±6、3 次 ±8…）。
- **勝方 +、敗方 −**，同一個數字。
- **僅雙方都用攻擊型技能時**才有變化；一方用防禦型、或單方面攻擊 → 雙方都不變。
- **結算時機**：拚點過程中專注力不動（全程用拚點開始前的值擲幣），**拚點結束後一次結算**。

### 6d 特殊硬幣（允許同一技能混搭類型）

- 技能 = 一串**有順序**的硬幣，每枚標型別。
- **輸一次交換** → 從序列第一枚移除一枚，移除方式看型別：
  - 普通／綠 → **損失**（消失）。
  - 紅 → **碎幣**（不算消失，記錄起來，等拚點後追加攻擊）。
- 算最終威力時只有「**還在的硬幣**」參與（碎幣同樣讓可擲硬幣變少）。
- **碎幣追加攻擊**：**只有這把技能「拚輸」時才觸發**。把所有碎掉的紅幣做一次**單方面追加攻擊**（不再拚點），打原拚點目標：
  - 基礎威力視同 1、每枚硬幣威力視同 +1；**傷害乘數照常套用**；逐枚擲、正面累加。
  - 雙方若都拚輸且有碎紅幣，各自做各自的。
  - 拚**贏**的一方就算過程中碎了紅幣，**不做**追加攻擊（碎紅幣血本無歸）。
- **綠幣對消**（拚點開始前先做）：
  - 只在「我方有綠幣 ∧ 對方技能帶紅幣」時觸發；對普通／綠幣時，綠幣視同普通幣。
  - 對消數量 = `min(我方綠幣數, 對方紅幣數)`；雙方各損該數量（各依型別順序由前往後）。
  - **被對消的紅幣是「損失」（消失），不是「碎幣」→ 不觸發追加攻擊**（與拚點中碎掉的紅幣不同，必須分開記）。
  - 雙向各自 min，順序無關。

---

## 7. 傷害結算公式

每枚**存活硬幣逐一**結算（含反面那枚——反面照打一下，只是不增加累計正面數；損失的硬幣完全跳過）：

```
單枚傷害 = ⌊ 最終威力 × 傷害乘數 × 攻擊方係數 × 目標方係數 ⌋
```

| 項 | 算法 |
|------|------|
| 最終威力 | 基礎威力 + (累計正面數 × 硬幣威力)，逐枚累加 |
| 傷害乘數 | 該技能武器的傷害骰，**過技能時擲一次（手動輸入）、整把技能共用** |
| 攻擊方係數 | `1 + 傷害強化(層×5%) − 傷害弱化(層×5%) + 凝神(該枚觸發則 +0.30)` |
| 目標方係數 | `1 + 混亂(目標處於則 +0.80) + 易損(層×5%) − 守護(層×5%)` |
| 取整 | **每下各自無條件退位** |

**疊乘方式 = 分組相乘**：攻擊方那組內部相加、目標方那組內部相加，**兩組再相乘**。

**逐枚迴圈內要做的事**：

1. 擲該枚硬幣（工具自動，加權正面率）→ 正面則累加最終威力。
2. **凝神判定**：丟 1D20（**手動輸入**）< 凝神級數 → 該枚 +30%，然後凝神級數 −2、層數 −1。
3. 算單枚傷害 → **先扣臨時生命值、再扣真實血量**。
4. **每扣完一次血，重新檢查黃金交叉**（是否跨過混亂值）。**若目標中途進入混亂**：
   - 同一次攻擊**剩下的硬幣立即吃 +80%**（目標方係數在攻擊中途可變動）；
   - 目標**立即**進入混亂——本回合剩餘行動權作廢，設 `confusionLockTurns = 2`（見 §8）。

> 因守護與傷害弱化的層數上限均為 16（= −80%），兩組係數各自最低只到 0.2，**永不為負**，不需另設下限。

---

## 8. 狀態觸發對照表（階段五）

### 有觸發時機的狀態

| 觸發時機 | 狀態 | 效果 | 觸發粒度 |
|------|------|------|------|
| 被攻擊時 | 萎靡 | 目標專注力 − 級數 | **每枚硬幣各觸發一次** |
| 被攻擊時 | 開裂 | 目標血量 − 級數 | **每枚硬幣各觸發一次** |
| 進行拚點時 | 失血 | 血量 − 級數 | **每次交換各觸發一次** |
| 使用技能擲硬幣時 | 恍惚 | 該枚硬幣威力歸零（層 −1） | 每枚 |
| 結算傷害時 | 凝神 | 見 §7 | 每枚 |
| 受對應「爆發／殘響」技能時 | 震顫、延燒(額外)、血瀑、炎瀑、荊棘瀑 | 依各自文本 | 依技能 |
| 回合結束時 | 延燒 | 血量 − 級數 | 每回合一次 |

- 每次觸發後該狀態層數 −1（除非文本另有規定）。
- **延燒在回合結束的層 −1，與通則的回合結束層 −1 視為同一次，不重複扣。**

### 無觸發、持續生效（用到時才讀值）

| 狀態 | 效果 | 讀取時機 |
|------|------|------|
| 強壯／虛弱 | 基礎威力 ± 層數 | 算最終威力時（拚點與傷害都吃） |
| 屏息／遲鈍 | 防禦基礎威力 ± 層數 | 算防禦最終威力時 |
| 守護／易損 | 受到傷害 ∓ 層×5% | 傷害結算（目標方係數） |
| 傷害強化／弱化 | 造成傷害 ± 層×5% | 傷害結算（攻擊方係數） |
| 迅捷／綁縛 | DEX ± 層×5%（上限 10 層） | DEX 判定／攔截時 |

### 狀態進入與恢復條件

兩者都「中途觸發」，但**進入時機不同**：混亂立即發作，恐慌延遲一回合。兩者都在第 3 回合恢復行動。

**混亂**
- **觸發瞬間**：血量跨過混亂值（黃金交叉）的當下（發生在階段四逐枚扣血時）。
- **進入**：**立即**。本回合剩餘的行動權**當場作廢**，並設 `confusionLockTurns = 2`。
- **維持**：下一回合的回合開始判定仍 `confusionLockTurns > 0` → 無法行動。
- **恢復**：`confusionLockTurns` 在每個階段六 −1，歸 0 後**自然恢復行動**（第 3 回合）。**恢復不做任何額外結算——血量不動**。
- 時間軸：第1回合中途進入 → 第1回合剩餘+第2回合無法行動 → 第3回合恢復。

**恐慌**
- **觸發瞬間**：專注力**跨越到 −40 的那一刻**（門鈴式，停留在 −40 不重複觸發）。
- **進入**：**延遲到下一回合**（設 `panicNextTurn`）。
- **維持**：恐慌回合無法行動（1 回合）。
- **恢復**：恐慌結束後設 `panicRecovering` → 再下一回合的回合開始時**專注力歸 0**（避免卡在 −40 死循環）。
- 時間軸：第1回合中途觸發 → 第2回合恐慌無法行動 → 第3回合恢復（並歸 0 專注力）。

> 兩者差異：**混亂立即發作、恢復時血量不動；恐慌延遲一回合、恢復時專注力歸 0。**

---

## 9. 攔截規則

- **本質 = 把敵方技能的目標，轉移到高 DEX 攔截者身上。**
- **資格**：攔截者 DEX **嚴格大於**被攔技能當前目標的 DEX（相等不行）。
- **拚點機會的三種來源**：
  1. 技能本來就指向我 → 直接拚（無關攔截）。
  2. 我 DEX 嚴格大於目標、主動攔截把目標搶過來 → 拚。
  3. 都不是 → 單方面挨打（除非自己用防禦型技能）。
- **成本**：攔截消耗一個槽位；一槽一場拚點；攔了就少一次自保。
- **多人搶攔同一目標**：DEX 最高者有最優先決定權；工具只呈現「誰有資格攔」，由玩家溝通、**KP 確認最終配對**。
- **方向限制**：**boss／敵方不能攔玩家**（避免 KP↔PL 最佳解無限互換）。
  - **例外**：敵方被動技能若自訂了攔截相關機制，**以被動描述為準**（引擎要允許被動覆寫此通則）。

---

## 10. 特殊狀態完整清單（含級／層分類）

> **重要修正**：原規則書把一批「簡單狀態」的效果寫成「× 級數」，但技能只用「層」施加，導致它們永遠級數 0、形同失效。本系統將這批改為**層數驅動**。

### 層數驅動（效果 = 層數 × 值）

`守護`、`易損`、`傷害強化`、`傷害弱化`、`迅捷`、`綁縛`、`強壯`、`虛弱`、`屏息`、`遲鈍`
（上限：守護那批 16 層；迅捷／綁縛 10 層）

### 級數驅動（真正用到級數，不變）

`凝神`、`震顫`、`萎靡`、`延燒`、`失血`、`開裂`、`血瀑`、`炎瀑`、`荊棘瀑`

### 其他

- `恍惚`：純層數（觸發使該枚硬幣威力歸零，層 −1）。
- `混亂` / `恐慌`：狀態條件，非層／級狀態（見 §8）。

---

## 11. 印記機制（NPC 專屬／共用標籤）

- **印記 = 自訂狀態，標成「純標籤、無內建效果」**，用唯一 **ID** 識別。
- 效果**不寫在印記定義裡**，而寫在「讀取它」的技能條件裡。
  - 例（木槿《仕女的關注》）：技能A「施加 2 層 `lady_gaze`」（貼）；技能B/C「若目標有 `lady_gaze` → 威力 +X」（讀）。
- 印記**共用層數管線**（回合結束 −1、1 層當回合消失…），引擎不需為它另寫生命週期。
- **專屬**：只有該 NPC 的技能引用此 ID。**共用**：多個 NPC 的技能引用同一 ID（可做「敵 A 上標、敵 B 引爆」）。
- **UI 上把印記與通用狀態視覺分開**（不同區塊／顏色），讓桌邊一眼分辨。
- 新增印記 = 填四步：① 給 ID 與顯示名 → ② 標為印記型 → ③ 技能裡寫施加 → ④ 技能裡寫讀取條件。**不需改引擎程式碼。**

---

## 12. KP 可填寫／自定義介面（四類）

### A. 每回合臨場填／操作

- NPC 槽位數（手填，無上限檢查）
- NPC 技能選擇（從抽到菜單挑）
- NPC 初始攻擊目標（工具隨機骰、KP 可覆寫）
- 隨機效果結果（工具自動骰、KP 可覆寫）
- 最終拚點配對確認

### B. 戰鬥前設定

- 參戰名單（PC + NPC）
- PC 槽位成長順序
- 初始狀態掛載（可預先給角色掛狀態，含指定層／級）

### C. NPC／敵人設計（完全自定義）

- 角色數值：HP、DEX、屬性、技能熟練度
- 技能：基威／幣威／硬幣數量／硬幣類型與排列順序／每枚硬幣效果／條件式／`drawable` 旗標
- **技能對照表（slotMap）預設 + slotMapRule 清單**：做形態轉換／技能替換／解鎖
- 被動技能：自由撰寫（引擎要支援掛載自訂被動邏輯）
- 攔截例外被動
- 印記定義
- 平衡微調參數

### D. 手動輸入骰值

- 技能抽選 1d6 ×2、傷害乘數骰、凝神 1d20、自訂被動裡的其他骰。

---

## 13. 擲骰處理

| 擲骰 | 處理方式 |
|------|------|
| 機率加權的硬幣（正面率 50%+專注力%） | **工具自動擲** |
| 隨機目標選擇 | 工具自動選、**KP 可覆寫**（非實體骰） |
| 技能抽選 1d6、傷害乘數骰、凝神 1d20、自訂被動骰 | **KP／玩家手動輸入** |

> 工具流程在「技能抽選、傷害骰、凝神」這幾個點**暫停等待手動輸入**，其餘自動往下算。

---

## 14. 開發功能列表

### MVP（第一階段，單機，先把計算做對）

- [ ] 實體／狀態資料模型與顯示面板（HP、有效 DEX、專注力、各狀態層/級）
- [ ] 槽位配發（PC 自動、NPC 手填）
- [ ] **技能對照表（slotMap）結算**：抽技能前重置並套用 slotMapRule（替換／解鎖）
- [ ] **喚出技能（invokeSkill 效果）**：繞過骰選直接打出 `drawable=false` 技能
- [ ] 技能抽選（1d6 手動輸入 ×2、經 slotMap、選一）
- [ ] 回合生命週期控制器（六階段，含階段六依序與豁免旗標）
- [ ] 拚點引擎（6a–6d，加權硬幣自動擲、平手重擲、混搭/紅綠碎幣、碎幣追加攻擊）
- [ ] 傷害引擎（分組相乘、逐枚、凝神、混亂中途重檢、無條件退位）
- [ ] 狀態觸發引擎（每枚／每次交換粒度、觸發對照表、延燒不重複扣）
- [ ] 攔截資格判定與配對（KP 確認）
- [ ] 手動骰值輸入提示（在正確時機暫停）
- [ ] 隨機效果自動骰 + KP 覆寫
- [ ] 印記（純標籤自訂狀態 + 技能讀取條件）
- [ ] NPC／技能／被動／印記的資料輸入介面

### 第二階段（加同步）

- [ ] Firebase 房間（KP 建房、玩家輸房號加入）
- [ ] 角色與權限（正式狀態只有 KP 寫、玩家寫自己宣告）
- [ ] 即時同步／廣播
- [ ] 斷線重連、重整後取最新狀態
- [ ] 權威裁定流程（玩家送宣告 → KP 裁定 → 寫入）

### 加分項

- [ ] 狀態變化動畫
- [ ] 戰鬥事件 log／歷史
- [ ] 印記與通用狀態的視覺區分強化

---

## 附錄：實作上最容易寫錯的五個點

1. **戰鬥輪 vs 回合**：兩個獨立計數器，專注力只在戰鬥輪開始歸 0。
2. **目標方係數在同一次攻擊中可變**：每枚硬幣後重檢黃金交叉，中途進混亂則後續硬幣 +80%。
3. **碎幣 vs 損失**：拚點中碎掉的紅幣 → 拚輸時做追加攻擊；綠幣對消掉的紅幣 → 純損失、無追加。兩者分開記。
4. **回合結束豁免**：步驟 2 才加上的狀態，不吃步驟 3 的層 −1。
5. **觸發粒度**：萎靡／開裂每枚硬幣、失血每次交換——都按最細粒度，且每次觸發各自層 −1。
